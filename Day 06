## 🔹 Problem Title
**Sum of Divisors of Numbers with Exactly Four Divisors**
## 🧩 Problem Description
Given an integer array `nums`, return the **sum of divisors** of the integers in that array that have **exactly four divisors**.
If no such integer exists, return `0`.

## 🧪 Example
**Input**
nums = [21, 4, 7]
**Output**
32
**Explanation**
* 21 → divisors: 1, 3, 7, 21 → sum = 32 ✅
* 4 → 3 divisors ❌
* 7 → 2 divisors ❌
Only `21` satisfies the condition.

## 💡 Approach
1. Traverse each number in the array
2. Find all its divisors by iterating from `1` to `√n`
3. Count divisors and calculate their sum
4. If divisor count becomes greater than `4`, stop early
5. Add the sum only if the count is exactly `4`

## ✅ Java Solution

class Solution {
    public int sumFourDivisors(int[] nums) {
        int totalSum = 0;

        for (int num : nums) {
            int count = 0;
            int sum = 0;

            for (int i = 1; i * i <= num; i++) {
                if (num % i == 0) {
                    int d1 = i;
                    int d2 = num / i;

                    if (d1 == d2) {
                        count++;
                        sum += d1;
                    } else {
                        count += 2;
                        sum += d1 + d2;
                    }

                    if (count > 4) break;
                }
            }

            if (count == 4) {
                totalSum += sum;
            }
        }

        return totalSum;
    }
}
## ⏱️ Complexity Analysis
* **Time Complexity:** `O(n × √m)`
* **Space Complexity:** `O(1)`

