# Concatenation of Array

## Problem
Given an integer array nums of length n, create an array ans of length 2n such that:
- ans[i] = nums[i]
- ans[i + n] = nums[i]
## Code
class Solution {
    public int[] getConcatenation(int[] nums) {
        int n = nums.length;
        int[] ans = new int[2 * n];

        for (int i = 0; i < n; i++) {
            ans[i] = nums[i];
            ans[i + n] = nums[i];
        }
        return ans;
    }
}


## Example
Input: [1,2,1]  
Output: [1,2,1,1,2,1]

## Approach
- Create a new array of size 2n
- Copy nums into first half
- Copy nums again into second half

## Time Complexity
O(n)

## Space Complexity
O(2n)

## Language
Java
